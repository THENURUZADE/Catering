<UserControl x:Class="Catering.Views.Components.ButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Catering.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Margin="2" Style="{StaticResource btnAdd}"/>
        <Button Grid.Row="1" Margin="2" Style="{StaticResource btnUpdate}"/>
        <Button Grid.Row="2" Margin="2" Style="{StaticResource btnReject}"/>
        <Button Grid.Row="3" Margin="2" Style="{StaticResource btnDelete}"/>
    </Grid>
</UserControl>
